name: keep-alive
on:
  schedule:
    - cron: '*/4 * * * *'
  workflow_dispatch:
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Railway backend
        run: curl -sf --max-time 10 ${{ secrets.RAILWAY_BACKEND_URL }}/healthz || echo "Backend unreachable"
